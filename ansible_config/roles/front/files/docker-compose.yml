
services:
  frontend:
    
    image: mahmoudkhera/front
    container_name: inventory-frontend
    restart: on-failure
    ports:
      - "${FRONTEND_PORT}:80"
    environment:
      - HOST=0.0.0.0
      - PORT=80
      - API_URL=${API_URL}